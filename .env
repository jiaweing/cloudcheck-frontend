# Auth Service
NEXT_PUBLIC_ENVOY_URL=http://envoy:8080

# Image Checker Service
NEXT_PUBLIC_IMAGE_CHECKER_SERVICE_URL=http://image-checker:8081

# General
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Demo Mode (set to 'true' to enable demo mode with default credentials)
NEXT_PUBLIC_DEMO_MODE=true

# Note: 
# - For local development, use localhost instead of service names
# - For production, update URLs to match your deployment configuration
# - When running in Docker, use the service names defined in docker-compose
# - Set DEMO_MODE to false in production
